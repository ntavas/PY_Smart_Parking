/**
 * =======================================================================
 * vite-env.d.ts - Τύποι TypeScript για το Vite Build Tool
 * =======================================================================
 *
 * ΤΙ ΚΑΝΕΙ ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ:
 *   Δηλώνει στο TypeScript ότι χρησιμοποιούμε το Vite και τους τύπους του.
 *   Χάρη σε αυτό το αρχείο, μπορούμε να χρησιμοποιούμε:
 *   - import.meta.env.VITE_API_BASE (μεταβλητές περιβάλλοντος)
 *   - import.meta.hot (hot module replacement)
 *   - Εικόνες και assets ως modules (import logo from './logo.png')
 *
 * ΓΙΑΤΙ ΧΡΕΙΑΖΕΤΑΙ:
 *   Το TypeScript δεν γνωρίζει από μόνο του τι είναι το import.meta.env.
 *   Αυτή η δήλωση (<reference types="vite/client" />) λέει στο TypeScript:
 *   "Κοίτα τους τύπους του package vite/client για να μάθεις τι υπάρχει".
 *
 * ΑΥΤΟΜΑΤΟ ΑΡΧΕΙΟ:
 *   Δημιουργείται αυτόματα από το Vite κατά το setup.
 *   Δεν χρειάζεται τροποποίηση.
 * =======================================================================
 */

/// <reference types="vite/client" />
